{
	<% begin %>
Street, Citystatezip, <% @outputs.find_by(id: @start.to_f).names.to_s.gsub("[","").gsub("]","").gsub("\"","").split(",").each do |name| %><%= name.to_s + " pass," %><% end %><% @outputs.find_by(id: @start.to_f).names.to_s.gsub("[","").gsub("]","").gsub("\"","").split(",").each do |name| %><%= name.to_s + " numbers," %><% end %><% for qcount in 0..11 %><%= "Reason "+ qcount.to_s + "," %><% end %><% @outputs.find_by(id: @start.to_f).names.to_s.gsub("[","").gsub("]","").gsub("\"","").split(",").each do |name| %><%= name.to_s + " comments," %><% end %>--THE-END--
<% @outputs.each do |output|  %>
<% if output.id.to_f <= @end.to_f && output.id.to_f >= @start.to_f %>
<%= output.street.to_s %>, <%= output.citystatezip.to_s %>, <%= output.passes.to_s.gsub("[","").gsub("]","").gsub("\"","").gsub("\"","") %>, <%= output.numbers.to_s.gsub("[","").gsub("]","").gsub("\"","").gsub("\"","") %>, <%= output.reason.to_s.gsub("[","").gsub("]","").gsub("\"","").gsub("\"","") %>, <%= output.comments.to_s.gsub("[","").gsub("]","").gsub("\"","").gsub("\"","") %>, --THE-END--
<% end %>
<% end %>
<% rescue %> Not Found in PDQ <% end %>}